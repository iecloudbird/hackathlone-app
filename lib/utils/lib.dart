// Comment out for now, as I'm working on other parts of the app

// import 'dart:async';
// import 'package:uni_links/uni_links.dart';

// void handleDeepLink() async {
//   StreamSubscription? sub = linkStream.listen((String? link) {
//     if (link != null) {
//       final uri = Uri.parse(link); // e.g., myapp://signup?token=abc123
//       final token = uri.queryParameters['token'];
//       if (token != null) {
//         // Navigate to sign-up screen with token
//       }
//     }
//   });
// }
